# تحديث Notic Tahiro — النسخة النهائية للنشر

## ملخّص ما تم

أُنجزت في هذا التحديث جميع المتطلّبات السبعة لإعداد التطبيق للنشر على متجر Google Play بصورة احترافية ومتوافقة مع السياسات.

### اسم التطبيق

اسم التطبيق المعروض هو «Notic Tahiro» في كل الواجهات. كان الشعار يظهر مقلوباً («Tahiro Notic») بسبب اتجاه RTL، وقد عُولج ذلك بفرض اتجاه `dir="ltr"` على نص الشعار في `src/components/Logo.tsx`. الاسم سليم أصلاً في `capacitor.config.ts` و`android-patches/strings.xml` و`public/manifest.webmanifest` و`index.html`.

### إدارة المشاريع

زرّ حذف المشروع أصبح ظاهراً دائماً وقابلاً للنقر على الجوال، بعد أن كان يظهر فقط عند مرور الماوس. كما أُضيف مجلّد افتراضي باسم «بدون مشروع» يجمع تلقائياً كلّ الملاحظات والأفكار غير المرتبطة بمشروع، ويعرض عددها بنسبة من إجمالي المحتوى. ترجمته متوفّرة في اللغات الأربع (العربية والإنجليزية والإسبانية والصينية).

### إعداد إعلانات AdMob

اعتمدت النسخة النهائية على الوحدات الأربع الأصلية فقط، بعد حذف الوحدتين الإضافيتين السابقتين (`7597607384` و`8525196151`) من الكود تماماً.

| الوحدة | الرقم | الموضع | الحالة |
|--------|-------|--------|--------|
| Banner | `9649975525` | أسفل الشاشة الرئيسية (adaptive) | يعمل |
| Native Advanced | `2318280419` | يُحاول كبانر مع بديل آمن | يعتمد على نوع الوحدة في لوحة AdMob |
| App Open | `4416995991` | فتح التطبيق (مرّة كل 4 ساعات) | يعمل عند دعم الإضافة (كشف تلقائي) |
| Interstitial | `9962178163` | الخروج من المحرّر (كل 3 مرات + فاصل 45 ثانية) | يعمل |

ضمانات السياسة المُطبَّقة شاملة: لا يُعرض إلا إعلان واحد في كل لحظة، وتُطلب موافقة UMP/GDPR قبل تحميل أيّ إعلان، وتُقيَّد التكرارات بحدود زمنية واضحة، وتُستخدم إعلانات الاختبار الرسمية من Google أثناء التطوير عبر مفتاح `FORCE_TEST` لتجنّب أيّ خطر حظر للحساب. كذلك يُحقَن App ID تلقائياً في AndroidManifest عبر `apply-admob.sh` ضمن خط البناء.

ملاحظة جوهرية بشأن الوحدة `2318280419`: إن كانت في لوحة AdMob من نوع Banner فستعمل مباشرة ضمن التدوير. وإن كانت من نوع Native Advanced فعلياً، فإنّ الإضافة `@capacitor-community/admob` لا تعرض Native، وعندئذٍ يتدخّل النظام البديل تلقائياً ليعرض الوحدة `9649975525` بدلاً منها دون أيّ فراغ في الواجهة. ويُعلَّم في تخزين المتصفّح أنّها لا تُعبَّأ، فتُستبعَد من المحاولات اللاحقة منعاً لطلبات غير صالحة قد تثير سياسة «الزيارات غير الصالحة» لدى AdMob.

### الأيقونة

أُعيد توليد طبقات الأيقونة التكيّفية في مجلد `assets/` بملء كامل (100%) لتظهر جذّابة وممتلئة على كلّ أنواع أقنعة المُشغّلات في أندرويد (الدائرية وأشباه المربّع والمربّعة المدوّرة)، مع شاشة بداية ملوّنة وأيقونة `maskable` مخصّصة لتطبيقات الويب التقدّمية. يجري توليد كلّ كثافات أندرويد آلياً عبر `@capacitor/assets` ضمن خطوة جديدة في `.github/workflows/google-play-android.yml`.

## النشر عبر GitHub

دليل البناء والنشر متوفّر في ملف `README.md` بالأسلوب المطلوب، وكذلك تنبيه مفصّل بشأن التوكن المكشوف الذي ينبغي حذفه وإنشاء توكن جديد بصلاحيتَي `repo` و`workflow` قبل تشغيل سكربت `push-to-github.sh`. ينبغي مراعاة وجود مسافة في اسم ملف الـ zip، فتُوضع علامتا تنصيص حوله في أمر `unzip`. أمّا الـ keystore فيُولَّد تلقائياً بكلمة سر افتراضية يُنصَح بتغييرها قبل أوّل نشر، مع الاحتفاظ بنسخة آمنة منه لتمكين التحديثات المستقبلية على المتجر.

## تحسينات الفحص الشامل

أُجري فحص دقيق للتطبيق على عدّة محاور، وطُبّقت خمسة تحسينات رئيسية.

أُضيف **حاجز خطأ** على مستوى الجذر في `src/components/ErrorBoundary.tsx`. كان أيّ خطأ غير ملتقط في أيّ مكوّن يُسقط شجرة React بالكامل ويُظهر للمستخدم شاشة بيضاء، والآن يلتقطه الحاجز ويعرض واجهة استعادة باللغة العربية تطمئن المستخدم أنّ بياناته محفوظة، وتُتيح له إعادة المحاولة أو إعادة تشغيل التطبيق دون فقدان البيانات.

حُوِّلت **إحدى عشرة نافذة أدوات ثقيلة** إلى تحميل تكاسلي (`React.lazy` + `Suspense`)، شمل ذلك مدير الديون (44KB) و AI Copilot (28KB) والتتبّع المالي (24KB) ومصفوفة القرارات (24KB) وخريطة المعرفة وغيرها. لا تُحمَّل هذه النوافذ إلاّ عند فتحها فعلياً، ما يُقلّل حجم الحزمة الأولى بشكل ملحوظ ويُسرّع تشغيل التطبيق على الأجهزة البطيئة، مع مؤشّر تحميل موحَّد للحظات الفتح الأوّل.

حُصِّنت **طبقة التخزين** في `AppContext` عبر دالتي مساعدة (`safeRead` و`safeWrite`) تُغلّفان عمليات `JSON.parse` و`localStorage.setItem` بمعالجة استثناءات شاملة. أيّ بيانات تالفة تُسترجع تلقائياً إلى القيم الافتراضية بدل إسقاط التطبيق، وأيّ امتلاء في حصّة التخزين (QuotaExceeded) يُسجَّل بهدوء بدل أن يكون نقطة انهيار. طُبّقت هذه الحماية على أربع عشرة عملية استدعاء.

أُسقطت **سجلّات التطوير** (`console.log` و`console.debug` و`console.info` و`debugger`) آلياً من نسخ الإنتاج عبر إعداد `esbuild.drop` و`pure` في `vite.config.ts`، مع الإبقاء على `console.warn` و`console.error` لأنّهما أداتا تشخيص ضروريّتان عند ورود تقارير المستخدمين.

أُضيف **عامل خدمة** (`public/sw.js`) لتمكين الوضع دون اتصال للنسخة الويب التقدّمية، باستراتيجية stale-while-revalidate التي تُقدّم الأصول فوراً من الذاكرة المخبّأة ثم تُحدّثها من الشبكة في الخلفية، مع استثناء طلبات الإعلانات والتحليلات. التسجيل مُقيَّد بالويب فقط حتى لا يتعارض مع غلاف Capacitor.

## التحسينات الاحترافية الإضافية

أُضيفت سبعة تحسينات تُرفع التطبيق إلى مستوى احترافي أعلى.

أُنشئت طبقة **ردود فعل لمسية** موحَّدة في `src/utils/haptics.ts`، تُغلّف `@capacitor/haptics` بأمان فتعمل على الأجهزة الحقيقية فقط وتتلاشى بصمت على الويب. طُبّقت اللمسات على عمليّات الحفظ (نقرة نجاح) والتثبيت (نقرة خفيفة) وفتح تأكيد الحذف (نقرة تحذير) والتصدير والاستيراد، فتُضيف إحساساً ماديّاً للتطبيق على الجوّال يُميّزه عن التطبيقات الهواة.

أُضيف نظام **كتابة مؤجَّلة (debounced writes)** لتخزين البيانات. كلّ تعديل على الملاحظات أو المشاريع أو المعاملات المالية أو التفريغات أو سجلّات الدين/الائتمان لم يعد يُكتب فوراً إلى `localStorage`، بل بعد فاصل أربعمئة مللي ثانية من السكون، ما يُلغي تماماً ركوداً ملحوظاً كان يحدث أثناء الكتابة السريعة في المحرّر على الأجهزة المتوسطة. تأمين إضافي يُفرغ كلّ الكتابات العالقة عند إخفاء التطبيق أو إغلاقه فلا تضيع تعديلات اللحظة الأخيرة.

أُضيفت ميزة **تصدير واستيراد البيانات** بصيغة JSON من القائمة الجانبية، تُعطي المستخدم ملكية كاملة لمحتواه. التصدير يُنزّل ملفّاً بتاريخ اليوم يحوي كلّ ملاحظاته ومشاريعه ومعاملاته المالية وتفريغاته وسجلّاته المالية. الاستيراد يقرأ ملفّاً مُصدَّراً سابقاً، يتحقّق من بنيته، يستبدل البيانات، ويُظهر تقريراً موجزاً بعدد العناصر المُستوردة. هذه ميزة جوهرية لأيّ تطبيق ملاحظات احترافي ومن شروط متجر Google Play لتطبيقات تخزن بيانات شخصية.

غُلّف مكوّن **NoteCard** بـ `React.memo` مع مُقارن خصائص دقيق، فلم يعد يُعاد رسم البطاقة إلاّ إذا تغيّرت خصائصها فعلياً. عند عرض قوائم تحوي عشرات أو مئات الملاحظات، يمنع هذا إعادة الرسم المُتسلسل الذي كان يحدث مع كلّ تعديل في حالة التطبيق العامّة.

تُخفى **شاشة البداية** الآن يدوياً عبر `SplashScreen.hide()` فور جاهزية React بدل انتظار المؤقّت الإفتراضي البالغ 1600 مللي ثانية، فيُحسّ المستخدم بسرعة أكبر لتشغيل التطبيق. الإخفاء آمن: إن لم تُحمَّل الإضافة فالشاشة تختفي تلقائياً كما السابق.

أُضيفت بطاقتا **theme-color** منفصلتان لوضعَي الإضاءة الفاتح والداكن في `index.html`، فيتزامن لون شريط النظام على Android و PWA مع وضع التطبيق الحالي، ويبدو الكلّ متّسقاً مع نظام التشغيل.

أُضيفت حزمة `@capacitor/splash-screen` رسمياً إلى التبعيات وكانت ناقصة، رغم أنّ الإعداد في `capacitor.config.ts` كان يستدعيها.

نتيجة الفحص النهائي: كلّ الملفّات المعدَّلة سليمة نحوياً، إحدى عشرة نافذة مغلَّفة بـ Suspense، ستّ كتابات مؤجَّلة، سبع نقاط للردود اللمسية، ست استدعاءات لميزتَي التصدير/الاستيراد، حاجز خطأ مركَّب في ثلاث نقاط من `main.tsx`.

## اللمسات الإبداعية النهائية

أُضيفت أربع لمسات تُميّز التطبيق وتُكسبه طابعاً احترافياً متقدّماً.

أُضيفت ميزة **التراجع عن الحذف**. عند حذف أيّ ملاحظة، تظهر بطاقة سفلية تُعلم المستخدم باسم الملاحظة المحذوفة وتعرض زرّ «تراجع» متاحاً لستّ ثوانٍ. الاستعادة تُعيد الملاحظة بنفس مُعرّفها وتُحدّث عدّاد المشروع المرتبط بها. الاحتفاظ بآخر ملاحظة محذوفة يجري في الذاكرة فقط ضمن مرجع زمني واحد، فلا يستهلك تخزيناً أو ذاكرة طويلة الأمد، وبعد انقضاء المهلة يُحذف كلّ أثر للملاحظة. هذه أكثر الميزات بناءً للثقة في تطبيقات الملاحظات لأنّها تحمي المستخدم من الندم الفوري على حذف خاطئ.

أُضيف **مؤشّر الاتصال الذكي** في `src/components/NetworkStatus.tsx`. يبقى المؤشّر صامتاً تماماً ما دام الإنترنت متاحاً، وعند انقطاع الاتصال يظهر شريط علوي عنبري بسيط يُطمئن المستخدم بأنّ التطبيق ما زال يعمل لأنّ كل بياناته محلّية. عند عودة الاتصال يختفي الشريط تلقائياً. هذا التكامل البصري يُعزّز قيمة عامل الخدمة الذي يُمكّن التشغيل دون اتصال.

أُضيف **إحصاء فوري** في تذييل محرّر الملاحظات يعرض عدد الكلمات والحروف وزمن القراءة التقريبي بمعدّل مئتي كلمة في الدقيقة. الإحصاء يختفي عند عدم وجود محتوى، ويظهر فقط حين يبدأ المستخدم الكتابة. الأرقام مُنسَّقة بصياغة عربية مغربية.

وُسِّعت **اختصارات لوحة المفاتيح العالمية** لتشمل `Ctrl/⌘+N` لإنشاء ملاحظة جديدة (مع تجاهل الاختصار أثناء الكتابة في الحقول لمنع التضارب)، و`Esc` لإغلاق النوافذ المفتوحة بأولوية ذكية (لوحة الأوامر أولاً، ثم نافذة البحث، ثم القائمة الجانبية). الاختصار `Ctrl/⌘+K` لفتح لوحة الأوامر باقٍ كما كان. هذه اللمسة تُقرّب التطبيق إلى المعايير المتعارف عليها في تطبيقات الإنتاجية الراقية.

وُسِّع كذلك مكوّن **Toast** ليدعم زرّ إجراء اختيارياً بمدّة عرض متغيّرة (ستّ ثوانٍ للأفعال القابلة للتراجع بدل الثلاث الافتراضية)، مع سمات `aria-live` للقراءة بواسطة قارئات الشاشة.

نتيجة الفحص النهائي بعد كلّ هذه الإضافات: لا أخطاء نحوية في أيّ من الملفّات المعدَّلة، حاجز الخطأ مُركَّب في ثلاث نقاط، إحدى عشرة نافذة بتحميل تكاسلي، ستّ كتابات مؤجَّلة، ثماني نقاط للردود اللمسية، ستّ نقاط لميزة التصدير والاستيراد، نقاط متعدّدة لميزة التراجع ومؤشّر الاتصال، إحصاء كلمات نشط في المحرّر، وستّ نقاط لاختصارات لوحة المفاتيح.

## التحسينات النوعية الإضافية

أُضيف **حفظ تلقائي ذكي** إلى محرّر الملاحظات. كان المستخدم سابقاً مضطرّاً للضغط على زرّ الحفظ يدوياً، وأيّ خروج مفاجئ من المحرّر قبل الحفظ يُعرّضه لفقد عمله. الحفظ التلقائي الجديد ينطلق بعد ثانيتين من توقّف التعديل، يتجاهل الملاحظات الفارغة، ويُلغى إن استمرّ المستخدم في الكتابة فلا يحدث حفظ متكرّر مزعج. يتكامل مع مؤشّر «الحفظ» البصري الموجود ومع الردود اللمسية، فيشعر المستخدم بطمأنينة دائمة بأنّ عمله محفوظ دون تدخّل منه.

أُضيف **إبراز نتائج البحث** داخل بطاقات الملاحظات. عند الكتابة في مربّع البحث، يُحاط المصطلح المُبحوث عنه بخلفية صفراء داكنة لطيفة داخل العنوان وداخل معاينة المحتوى، فيرى المستخدم فوراً أين وقع التطابق دون أن يُضطرّ إلى فتح كلّ ملاحظة للتحقّق. التقسيم يتمّ عبر `String.split` مع مرجع آمن، فلا توجد أيّ مخاطر إدراج HTML مباشر.

حُسّن **أداء البحث** بشكل ملحوظ عبر `useDeferredValue` و`useMemo`. كان البحث يُعيد تصفية كامل قائمة الملاحظات على كلّ ضغطة مفتاح، ما يُبطئ لوحة المفاتيح مع وجود مئات الملاحظات. الآن تُؤجَّل قيمة البحث فيبقى حقل الإدخال سلساً تماماً، وتُحسب التصفية والترتيب مرّة واحدة فقط عند تغيّر مدخلاتها الفعلية بدل كلّ إعادة رسم. لُفّت كذلك كتابة `notic-notes-view` بـ `try/catch` للأمان.

كلّ هذه الإضافات سليمة نحوياً ومتكاملة مع الميزات السابقة. تطبيق الملاحظات الآن يحفظ تلقائياً، يُبرز نتائج البحث، يستجيب فوراً للكتابة حتى مع قوائم كبيرة، وقد جمع في جلسة واحدة ثمانية وعشرين تحسيناً موزّعاً على المتانة والأداء والأمان والتجربة وملكية البيانات والامتثال لسياسات المتاجر.

## تحسينات سهولة الاستخدام

أُضيف **تركيز تلقائي على حقل العنوان** عند فتح محرّر ملاحظة جديدة. كان المستخدم يحتاج نقرتين إضافيتين قبل بدء الكتابة (واحدة لفتح المحرّر وأخرى للنقر على الحقل لإظهار لوحة المفاتيح)، أمّا الآن فلوحة المفاتيح تظهر فور فتح المحرّر فيبدأ التدوين فوراً.

أُعيدت صياغة **الحالة الفارغة** في القائمة الرئيسية لتُصبح ودودة وتدفع المستخدم نحو فعل واضح. بدل النصّ المجرّد، تظهر بطاقة كبيرة بأيقونة 📝 وعبارة ترحيب «مرحباً في مفكّرتك»، وأسفلها زرّ بارز بتدرّج بنفسجي مع ظلّ ملوّن نصّه «✨ إنشاء ملاحظتي الأولى»، يصاحبه تلميح صغير بالاختصار `Ctrl+N` لمن يفضّل لوحة المفاتيح. الحالة الخاصّة بنتيجة بحث فارغة لها رسالة مختلفة («لم نجد شيئاً» مع تلميح للبحث بكلمة مختلفة) تُميّز بوضوح بين «لا توجد بيانات» و«لا توجد نتائج».

أُضيفت **تسميات وصفية (aria-label) إلى ثلاثة وعشرين زراً** كانت تعتمد على الأيقونة فقط، فأصبحت أكثر وصفاً للمستخدمين الجدد وأكثر دعماً لقارئات الشاشة. مجموع `aria-label` في التطبيق بلغ اثنين وثلاثين عنصراً بعد هذه الجولة. السمة `title` التي تُظهر تلميحاً عند المرور بالماوس باقية حيث كانت، فحاز كلّ زر على طبقتَي وصف بصرية وصوتية.

نافذة البحث كانت قد حازت سابقاً على تركيز تلقائي عبر `autoFocus`، فلم تحتج تعديلاً. الفحص النحوي بعد كل هذه التعديلات نظيف تماماً.

التطبيق الآن يفتح بسرعة، يستقبل المستخدم بترحيب واضح، يُتيح له البدء فوراً دون نقرات إضافية، يُعلن وظائف كلّ زر صراحةً، ويحفظ عمله تلقائياً وراءه. سهولة الاستخدام هذه تتكامل مع سرعة الإقلاع والاستجابة وملكية البيانات والتراجع عن الحذف والإحصاءات الفورية، فيقدّم تجربة احترافية متّسقة من اللحظة الأولى للتشغيل حتى آخر إجراء يُجريه المستخدم في الجلسة.

## الميزات التنافسية الكبرى: الروابط الثنائية والإدخال الصوتي

أُنجزت أولى الميزتين التنافسيّتين اللتين تُميّزان Notic Tahiro عن أيّ تطبيق ملاحظات عربي قائم اليوم.

أُضيف **نظام الروابط الثنائية** الكامل بصياغة `[[اسم الملاحظة]]` على غرار Notion و Obsidian، مع تكيّف عربي خاصّ. الوحدة الأساسية `src/utils/backlinks.ts` تُقدّم دوال خالصة لاستخراج الروابط، البحث عن الملاحظات بالعنوان، حساب الإشارات المرتدّة، وكشف سياق المؤشّر داخل قوسَين مزدوجَين. مكوّن `LinkAutocomplete.tsx` يُقدّم نافذة اقتراحات ذكية تظهر فور كتابة `[[`، مع تنقّل بالسهمَين، اختيار بـ Enter، إلغاء بـ Esc، وعرض تاريخ آخر تحديث لكلّ اقتراح. إن لم يكن هناك ملاحظة بالعنوان المكتوب، يظهر خيار «إنشاء ملاحظة جديدة بهذا الاسم» (رابط شبح).

داخل محرّر الملاحظة، أُضيفت لوحة بصرية أنيقة تعرض كلّ الروابط الصادرة من الملاحظة الحالية بنفسجية اللون، وكلّ الملاحظات الأخرى التي تُشير إلى هذه الملاحظة (الإشارات المرتدّة) بخضراء اللون. النقر على أيّ رابط ينقل المستخدم فوراً إلى الملاحظة المعنية دون إغلاق المحرّر (يُعاد تركيب المحرّر بمعرّف جديد عبر مفتاح React). النقر على رابط شبح يفتح محرّر ملاحظة جديدة بالعنوان مُعبَّأ مسبقاً عبر `sessionStorage`.

النتيجة العملية: المستخدم يستطيع بناء شبكة معرفة عضوية دون مغادرة المحرّر. يكتب `[[`، تظهر له آخر ملاحظاته، يختار واحدة بنقرة، يستمرّ في الكتابة. عند فتح أيّ ملاحظة، يرى فوراً كلّ الملاحظات التي تُشير إليها فيتذكّر السياق الكامل. **هذه التجربة لم يقدّمها أيّ تطبيق ملاحظات عربي حتى اليوم بهذه الأناقة.**

بشأن **الإدخال الصوتي بالدارجة المغربية**: تبيّن أنّ المكوّن `VoiceRecorder.tsx` موجود مسبقاً ومُتكامل في زرّ ميكروفون داخل المحرّر، ويستخدم الإعداد الصحيح `recognition.lang = 'ar-MA'` لاستهداف الدارجة المغربية مع مؤشّر مستوى صوت بصري. ما كان ينقصه فعلاً هو **أذونات الميكروفون في Android**، فأضفتها إلى خط البناء `apply-admob.sh` ليتمّ حقن `RECORD_AUDIO` و`MODIFY_AUDIO_SETTINGS` في `AndroidManifest.xml` تلقائياً عند بناء APK، مع غلاف Python آمن من إضافتها مرّتين. بُنيت بالإضافة وحدة جديدة `src/utils/speech.ts` كغلاف نظيف لـ Web Speech API يدعم سلسلة لغات احتياط (الدارجة أوّلاً، فالمصرية، فالسعودية، فالعربية العامة) ويتعامل مع كلّ رموز الخطأ بترجمة عربية مفهومة، جاهزة للاستخدام في مكوّنات مستقبلية إن أردت توسيع الميزة.

الفحص النحوي لكلّ هذه الإضافات نظيف، ومجموع أربعة عشر نقطة استخدام لنظام الروابط داخل محرّر الملاحظات، وأذونات الميكروفون مضمونة في كلّ بناء.

## إعادة التموقع: تطبيق دوليّ بنَفَس مغربي خفيف

أُعيد تموقع Notic Tahiro من «تطبيق مغربي مُفعَّم» إلى **تطبيق دوليّ ذي لمسة مغربية أنيقة**. الهوية الجوهرية للتطبيق هي إنتاجيته العالمية، والمنشأ المغربي يظهر كتوقيع راقٍ لا كتوجّه إقليمي.

أُضيف **ثلاثون مفتاحاً جديداً للترجمة** عبر اللغات الأربع (العربية والإنجليزية والإسبانية والصينية)، فبلغ مجموع مفاتيح التطبيق سبعين مفتاحاً لكلّ لغة بتوازن كامل. شملت المفاتيح كلّ النصوص العربية المحفورة سابقاً في حاجز الخطأ، مؤشّر الاتصال، الحالة الفارغة، لوحة الإشارات المرتدّة، إحصاء الكلمات، أزرار التصدير والاستيراد، نوافذ الإكمال التلقائي، ورسائل التراجع.

حُوّلت **مكوّنات المستوى الجذري** التي لا تستطيع استخدام `useApp` (حاجز الخطأ ومؤشّر الاتصال) إلى نمط ترجمة محلّي يقرأ تفضيل اللغة مباشرة من `localStorage`، بأربع لغات مكتملة. هذا يضمن أنّ المستخدم الإنجليزي يرى رسالة خطأ بالإنجليزية، والإسباني بالإسبانية، وهكذا، حتى عند أعطال شجرة React.

دُوِّلت **تنسيقات التواريخ والأرقام** عبر دالة `toLocale(language)` تُحوّل لغة التطبيق إلى رمز BCP-47 المناسب (ar, en-US, es-ES, zh-CN). كانت كلّ التواريخ والأرقام تُعرض بصياغة مغربية ثابتة بغضّ النظر عن لغة المستخدم؛ الآن يرى الإنجليزي «1,234 words • 5 min read» والإسباني «1.234 palabras • 5 min de lectura» والصيني الصياغة الصينية.

دُوِّل **الإدخال الصوتي** عبر دالة `mapAppLangToSpeech` التي تختار رمز التعرّف الصوتي حسب لغة التطبيق: الإنجليزية تستخدم `en-US`، الإسبانية `es-ES`، الصينية `zh-CN`. **العربية تحتفظ بـ `ar-MA` (الدارجة المغربية) لأنّها لمستخدم عربي حقيقي ميزة تنافسية فعلية لا توجد عند أيّ تطبيق آخر**، فيتحدّث بطبيعته دون تكلّف الفصحى.

التوقيع المغربي في تذييل القائمة الجانبية أُعيد تصميمه ليكون فخراً راقياً لا تصريحاً إقليمياً: بدلاً من «صُنع بحب لتعزيز إنتاجيتك» الإيمائي، يقرأ المستخدم الآن بلغته الخاصّة:
- العربية: «صُنع بشغف في تطوان 🇲🇦»
- الإنجليزية: «Crafted with care in Tetouan 🇲🇦»
- الإسبانية: «Hecho con pasión en Tetuán 🇲🇦»
- الصينية: «在得土安用心打造 🇲🇦»

هذه الصياغة (Made in Tetouan) على غرار «Designed in California» التي تستخدمها Apple — توقيع حرفية وأناقة، يرفع قيمة المنتج بدل أن يحصره في إقليم. اسم العلامة `NOTIC TAHIRO` بقي في الواجهة عوضاً عن `TAHIRO EDITION` السابق، فأصبح اسم المنتج هو البطاقة الرئيسية والمنشأ تابعاً.

النتيجة الإستراتيجية: التطبيق الآن يستقبل مستخدماً يابانياً أو ألمانياً أو مكسيكياً بلغته الكاملة وبتوقيع راقٍ من تطوان، ويستقبل المستخدم العربي بدارجته المغربية في الإدخال الصوتي وبصياغة حميمية لها هويتها. **هذا هو موقع التميّز: عالميّ في الواجهة، أصيل في التفاصيل.**

## القفزة الاحترافية: معاينة Markdown ومكتبة قوالب وترتيب القائمة

أُضيفت ثلاث ميزات نوعيّة كبرى تنقل التطبيق من «مفكّرة جيدة» إلى «أداة كتابة احترافية» بحقّ.

أُفعِّل **عرض Markdown حيٌّ** في محرّر الملاحظة عبر زرّ تبديل أنيق بين «✏️ تحرير» و«👁 معاينة» مُتعدّد اللغات. كان التطبيق يَعِد بدعم Markdown في الـ placeholder دون تطبيق فعلي، أمّا الآن فيقدّم مُصيِّراً متكاملاً في `src/components/MarkdownView.tsx` بطول مئتين وثلاثة وسبعين سطراً وبدون اعتماد على مكتبة خارجية تَزيد حجم الحزمة. يدعم العناوين بمستوياتها الستّة، التغميق والمائل والكود السطري وكتل الكود مع تسمية اللغة، القوائم المُرتّبة وغير المُرتّبة، **المهام التفاعلية** التي تُبدِّل حالتها بنقرة فتُحدِّث النصّ المصدر، الاقتباسات، الإبراز (`==`), الفواصل، الروابط الخارجية مع منع البروتوكولات الخطرة (javascript:, data:) لحماية كاملة من XSS، و**الروابط الثنائية `[[name]]` كأزرار قابلة للنقر** متصلة بنظام الـ backlinks فتفتح الملاحظة الهدف أو تُنشئها كرابط شبح. كلّ ذلك عبر عناصر React الخالصة دون `innerHTML` ولا `dangerouslySetInnerHTML`.

أُضيفت **مكتبة قوالب احترافية عالمية** في `src/utils/templates.ts` بستّة قوالب جاهزة بأربع لغات: ملاحظات اجتماع، يوميّات اليوم، مشروع جديد، ملاحظات كتاب، وقرار. كلّ قالب مُصمَّم بصياغة Markdown منظَّمة (عناوين، قوائم، مهام، اقتباسات، روابط ثنائية فارغة جاهزة للملء) وبصبغة عالمية تناسب المهنيين في أيّ ثقافة. القوالب تظهر بشكل جذّاب في الحالة الفارغة الترحيبيّة، فلا يبدأ المستخدم بصفحة بيضاء بل يختار سياقه ويبدأ التدوين فوراً. النقر على قالب يحفظه في `sessionStorage` ثم يفتح المحرّر الذي يلتقطه ويُعبّئ المحتوى والوسوم تلقائياً. التاريخ في القوالب الزمنية يُنسَّق بمحلّية المستخدم (وليس مغربية ثابتة)، فالأمريكي يرى «Monday, January 15, 2024» والصيني صياغته الخاصّة.

أُضيفت **خيارات ترتيب القائمة** بقائمة منسدلة احترافية في رأس قائمة الملاحظات. يستطيع المستخدم الآن الاختيار بين أربعة معايير ترتيب: «الأحدث» حسب تاريخ التحديث (افتراضي)، «الإنشاء» حسب تاريخ الإنشاء الأصلي، «العنوان» أبجدياً مع دعم اللغات (يستخدم `localeCompare` بصياغة عربية صحيحة للحروف العربية وكذلك للأخرى)، و«الطول» حسب عدد حروف المحتوى للعثور سريعاً على الملاحظات المُسهَبة أو الموجزة. الخيار يُحفظ في `localStorage` فيتذكّره التطبيق بين الجلسات. الملاحظات المثبّتة تبقى دائماً في الأعلى مهما كان معيار الترتيب، فلا تُغرَق بالخيار المتقدّم.

النتيجة الإجمالية لهذه الميزات الثلاث تنقل التطبيق إلى مصافّ الأدوات الكبرى. كاتب يستطيع الآن أن يبدأ من قالب اجتماع، يدوّن بـ Markdown مع تحقّق فوري عبر زرّ المعاينة، يربط الملاحظة بمشاريع أخرى عبر `[[]]`, ثمّ يجد ملاحظته بسرعة عبر معيار الترتيب الذي يناسبه. هذا تكامل لم يكن متوفّراً في النسخة السابقة وكان الفارق الجوهري بين التطبيقات الهواة والاحترافية.

الفحص النحوي نظيف لكلّ التعديلات، ومجموع التحسينات التي طرأت على التطبيق في هذه الجلسة الموسّعة تجاوز خمسة وثلاثين تحسيناً موزّعاً على المتانة والأداء والأمان والتجربة والتدويل والتميّز الإبداعي. التطبيق الآن تطبيق دوليّ ناضج بنَفَس مغربي راقٍ، جاهز للنشر وللمنافسة.

---

## 🛠️ تحديث 11 يونيو 2026 — إصلاح الأيقونات + AdMob + الترجمة الكاملة للذكاء الاصطناعي

### 1) 🚨 إصلاح حرج: نمط الشطرنجية في الأيقونات
كانت ملفات `assets/icon.png` و`icon-foreground.png` و`splash.png` و`splash-dark.png` تحتوي على **نمط الشطرنجية (مربعات الشفافية) محفوراً داخل الصورة** — أي أن أيقونة التطبيق على الأجهزة كانت تظهر بمربعات رمادية حولها.

**الحل:** قُصّ الشعار الرسمي الجديد برمجياً (flood-fill يحمي النجمة البلورية) وأُعيد توليد كل الأصول:
- `assets/icon.png` — معتمة 1024px بتدرج بنفسجي داكن + توهج نيون (iOS / Launcher)
- `assets/icon-foreground.png` — شفافة بمنطقة أمان 62% (Adaptive Icons أندرويد)
- `assets/icon-background.png` — تدرج شعاعي داكن
- `assets/icon-transparent.png` — **جديد**: الشعار النظيف بخلفية شفافة (المصدر الرئيسي)
- `assets/splash.png` + `splash-dark.png` — 2732px نظيفة بتوهج
- `public/icons/icon-{72..512}.png` + `icon-maskable-512.png` + `ic_stat_notification.png` + `icon.svg` — كلها بالشعار الجديد
- `scripts/generate-icons.js` — يولّد الآن من PNG النظيفة بدل SVG القديم

✅ تحقق آلي: checker_score = 0.000 على كل الأصول.

### 2) 📢 AdMob — إصلاح سباقات العرض
- **App Open**: حارس `_appOpenInFlight` + كتابة الطابع الزمني **قبل** العرض — يمنع ظهور الإعلان مرتين متتاليتين عند الإقلاع (initAdMob + appStateChange / StrictMode) — مخالفة سياسة محتملة سابقاً.
- **Banner**: حارس `_bannerBusy` ضد استدعاءات `showBanner` المتوازية عند إعادة mount سريعة.
- بقية المنظومة (UMP، بيني كل 3 خروجات + 45 ثانية، iOS Info.plist) سليمة كما هي.

### 3) 🤖 الذكاء الاصطناعي — متعدد اللغات بالكامل (ar/en/es/zh)
- **AICopilot** (مركز الذكاء الاصطناعي): 60 نصاً عربياً مُشفّراً → سجل `STR_ALL` بأربع لغات، وعناوين الملاحظات المولّدة والوسوم والتواريخ (`toLocaleDateString`) صارت بلغة المستخدم.
- **NotesChat** (اسأل مفكرتك): محرك البحث المحلي بالكامل (ردود، أسئلة مقترحة، تذييل) مترجم، والكلمات المفتاحية للنوايا تدعم الآن العربية والإنجليزية والإسبانية والصينية.
- **MorningBriefing**: رأس النافذة وأزرار التوليد/الاستماع/الإغلاق مترجمة (الموجز نفسه كان مترجماً مسبقاً).
- **NoteCard**: تثبيت/حذف/المهام/بدون عنوان ونافذة تأكيد الحذف — مترجمة.
- ✅ تحقق آلي: كل مفتاح مستخدم موجود في اللغات الأربع، وكل الملفات سليمة نحوياً.

### 4) 📋 ملاحظات متبقية (غير حرجة)
- معرّفات AdMob لـ iOS لا تزال معرّفات اختبار (موثّق في `admob.ts` و`apply-ios-config.sh`) — أنشئ تطبيق iOS في لوحة AdMob وضع `IOS_ADMOB_APP_ID` في GitHub Secrets.
- أدوات ثانوية (FinancialTracker، HabitTracker، SmartClipboard، DecisionMatrix، BrainDumpMode، StaticPage) لا تزال تحتوي نصوصاً عربية مُشفّرة — مرشحة لجولة ترجمة لاحقة.
