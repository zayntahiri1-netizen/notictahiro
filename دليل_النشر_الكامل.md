# 🚀 دليل النشر الكامل — Notic Tahiro

دليل عملي خطوة بخطوة لنشر التطبيق ودالة الذكاء الاصطناعي.

---

## القسم الأول — نشر دالة gemini-proxy عبر Supabase CLI

دالة `gemini-proxy` هي البروكسي الآمن الذي يخفي مفتاح Gemini عن المتصفح.
كل طلبات الذكاء الاصطناعي تمر عبرها.

### 1) تثبيت Supabase CLI (مرة واحدة)

```bash
# في Termux
npm install -g supabase

# تحقق من التثبيت
supabase --version
```

> إن فشل التثبيت العام في Termux، استخدم npx بدلاً منه في كل أمر:
> `npx supabase ...`

### 2) تسجيل الدخول وربط المشروع

```bash
cd ~/notic-tahiro-v2

# سجّل الدخول (سيفتح المتصفح للحصول على رمز)
supabase login

# اربط مجلدك المحلي بمشروع Supabase
# PROJECT_REF من: Supabase Dashboard > Settings > General > Reference ID
supabase link --project-ref oispxxmxltkmkcmoqxmy
```

### 3) ضبط الأسرار (Secrets)

```bash
# مفتاح Gemini من https://ai.google.dev (إجباري)
supabase secrets set GEMINI_API_KEY=ضع_مفتاح_Gemini_هنا

# سرّ حماية البروكسي (اختياري لكن موصى به بشدة)
# 1) ولّد سراً عشوائياً:
openssl rand -hex 24
# 2) ضع الناتج في الأمرين التاليين بنفس القيمة:
supabase secrets set APP_SHARED_SECRET=القيمة_العشوائية_المولّدة
# 3) وضع نفس القيمة في .env بالاسم VITE_APP_TOKEN

# تحقق من الأسرار المضبوطة
supabase secrets list
```

> SUPABASE_URL و SUPABASE_SERVICE_ROLE_KEY يُضبطان تلقائياً من Supabase — لا تضفهما يدوياً.

### 4) إنشاء جدول التتبع (مرة واحدة)

نفّذ ملف الهجرة `supabase/migrations/001_ai_requests.sql`:

```bash
# الطريقة الأولى (CLI):
supabase db push

# الطريقة الثانية (يدوياً):
# Dashboard > SQL Editor > الصق محتوى الملف > Run
```

### 5) نشر الدالة

```bash
# --no-verify-jwt ضروري: يسمح للتطبيق بالاستدعاء بدون توكن مستخدم مسجّل
supabase functions deploy gemini-proxy --no-verify-jwt
```

### 6) التحقق من نجاح النشر

```bash
# اختبار سريع (استبدل YOUR_PROJECT و TOKEN إن ضبطت APP_SHARED_SECRET)
curl -X POST \
  "https://oispxxmxltkmkcmoqxmy.supabase.co/functions/v1/gemini-proxy" \
  -H "Content-Type: application/json" \
  -H "x-app-token: نفس_قيمة_APP_SHARED_SECRET" \
  -d '{"prompt":"قل مرحبا بكلمة واحدة","maxTokens":20}'
```

الرد المتوقّع:
```json
{"text":"مرحبا","inputTokens":5,"outputTokens":2,"model":"gemini-2.5-flash"}
```

---

## القسم الثاني — تحديث ونشر التطبيق

### الطريقة (نفس أسلوبك المعتاد)

```bash
cd ~
mv notic-tahiro-v2 notic-tahiro-v2-old
unzip "/sdcard/Download/Notic Tahiro.zip"
mv "Notic_Tahiro" notic-tahiro-v2          # توحيد اسم المجلد
cd notic-tahiro-v2
cp ../notic-tahiro-v2-old/.env .env         # استرجاع إعداداتك السرية
chmod +x scripts/*.sh
./scripts/push-to-github.sh
```

> **ملاحظة مهمة:** اسم المجلد داخل ZIP هو `Notic_Tahiro` (بشرطة سفلية).
> سطر `mv "Notic_Tahiro" notic-tahiro-v2` يوحّده مع اسمك المعتاد.
> إن كنت تفضّل عدم إعادة التسمية، احذف هذا السطر واعمل داخل `Notic_Tahiro` مباشرة.

### ماذا يحدث بعد push-to-github.sh؟

1. يُنشئ/يُحدّث الـ repository على GitHub.
2. يولّد keystore للتوقيع إن لم يوجد.
3. يضيف أسرار GitHub Actions (AdMob وغيرها).
4. يرفع الكود ويُشغّل workflow البناء تلقائياً.
5. يبني APK/AAB موقّعاً وجاهزاً لرفعه على Google Play.

### بعد اكتمال البناء

- حمّل ملف `.aab` من GitHub Actions > Artifacts.
- ارفعه على Google Play Console.

---

## القسم الثالث — قائمة تحقق سريعة قبل النشر

- [ ] `.env` يحتوي `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` الصحيحة.
- [ ] `VITE_APP_TOKEN` في `.env` = `APP_SHARED_SECRET` في أسرار Supabase (إن استُخدم).
- [ ] دالة `gemini-proxy` منشورة وتعطي رداً صحيحاً في اختبار curl.
- [ ] جدول `ai_requests` موجود في قاعدة البيانات.
- [ ] `VITE_ADMOB_APP_ID` صحيح في `.env` (أو في أسرار GitHub).
- [ ] الأيقونات تُولّد تلقائياً عبر `@capacitor/assets` في الـ workflow.

---

## ملاحظات تقنية مهمة (محدّث يونيو 2026)

> ⚠️ **تحذير:** `gemini-2.0-flash` أُغلق نهائياً في 1 يونيو 2026 (يُعطي خطأ 404).
> المشروع مُحدَّث تلقائياً لاستخدام الجيل الجديد.

**النماذج المدعومة — سلسلة fallback ذكية من 4 نماذج مجانية:**

| النموذج | الحالة | الاستخدام في التطبيق |
|---------|--------|----------------------|
| `gemini-3.1-flash-lite` | ✅ **"3.1 Flash-Lite" — مجاني** | الافتراضي — وسوم، عناوين، أوامر، تلخيص، تدقيق |
| `gemini-3.5-flash` | ✅ **"3.5 Flash" — مجاني** | مسودات، تقارير، ترجمة، توسيع أفكار، محادثة |
| أي نموذج آخر | ❌ — | لا تستخدم (قديم أو مدفوع أو متوقّف) |

> نموذجان فقط — كلاهما من الجيل 3 الحالي كما يظهران في تطبيق Gemini.
> إن نفدت حصة الأول يتحوّل تلقائياً للثاني.

**عند نفاد الحصة المجانية:** البروكسي يرجع `QUOTA_EXCEEDED` (429) مع `Retry-After:60`،
التطبيق يعرض رسالة ودّية بلغة المستخدم. وطبقة `aiEngine` فيها **fallback محلي
لكل دالة** — فلو نفدت كل الحصص، يستمر التطبيق بالعمل بمنطق محلي بدلاً من الفشل.

**حدّ الطلبات:** 60 طلب/ساعة/IP — مُجزّأً (hashed) لحماية الخصوصية.

**thinkingBudget: 0** مضبوط للنماذج 2.5 — حاسم لتجنّب ردود فارغة وتكاليف مضاعفة.
