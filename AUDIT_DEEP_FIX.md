# 🔍 جولة التدقيق العميق — Notic Tahiro

تدقيق شامل واحترافي عبر أربعة محاور رئيسية مع إصلاح حذِر يحافظ على هيكل المشروع.

---

## 1) أيقونة التطبيق — شفافة تماماً لجميع الأجهزة ✅

### المشكلة الحرجة المكتشفة
الأيقونات القديمة (`assets/icon.png`، `icon-foreground.png`، `splash.png`، `splash-dark.png`)
كانت تحتوي على **نمط الشطرنجية الرمادي محفوراً داخل وحدات البكسل نفسها** — أي أن
المربعات الرمادية كانت جزءاً من الصورة وتظهر حول الأيقونة على أجهزة المستخدمين.
الصورتان المرفوعتان أيضاً كانتا RGB بلا قناة شفافية (الشطرنجية مدمجة فيهما).

### الحل
- **قص احترافي عالي الجودة** للشعار من الخلفية الرمادية باستخدام flood-fill ذكي
  يحمي النجمة البلورية البيضاء، مع إزالة تلوّث الحواف (decontamination) وحواف ناعمة
  مضادة للتسنّن. تم التحقق من النظافة على خلفية فاتحة وداكنة.
- **إعادة توليد كل الأصول شفافة** حيثما يسمح المعيار:
  - `assets/icon-transparent.png` — الماستر الشفاف (الشعار فقط، بلا خلفية)
  - `assets/icon-foreground.png` — شفافة، منطقة أمان 62% للأيقونة التكيّفية
  - `assets/icon-background.png` — تدرّج بنفسجي داكن (للطبقة الخلفية فقط)
  - `assets/icon.png` — معتمة (iOS واللانشر يتطلبان خلفية صلبة إجبارياً)
  - `splash.png` + `splash-dark.png` — **شفافة** لتظهر فوق خلفية Capacitor الملوّنة
    على أي نسبة شاشة (هاتف ضيّق → لوحة عريضة)
  - أيقونات PWA الكاملة (72→512) شفافة + maskable (منطقة أمان 72%)
  - أيقونة الإشعار البيضاء الشفافة + `icon.svg` يضمّن الشعار الشفاف
- **تحديث `scripts/generate-icons.js`** ليولّد من المصدر النظيف الجديد.

---

## 2) إعلانات AdMob — مناسبة لجميع الأجهزة ✅

- **حارس `_appOpenInFlight`**: يمنع التنفيذ المتوازي لإعلان App Open. عند الإقلاع
  قد يُستدعى مرتين بسرعة (`initAdMob().then()` + حدث `appStateChange` الأول، أو
  StrictMode في التطوير) فيظهر الإعلان مرّتين متتاليتين — مخالفة سياسة. الحارس يمنع ذلك.
- **كتابة الطابع الزمني قبل العرض**: حتى لو عُلّق `showAppOpen` أو فشل بعد العرض الفعلي،
  لا يتكرر الإعلان فوراً في المحاولة التالية.
- **حارس `_bannerBusy`**: يمنع استدعاءات `showBanner` المتوازية (إعادة mount سريعة).
- التحقق من سلامة UMP/GDPR، الإعلان البيني (كل 3 خروجات + 45 ثانية)، وإعدادات iOS.

---

## 3) الذكاء الاصطناعي ولغات التطبيق — أربع لغات كاملة ✅

تُرجم **كامل واجهة التطبيق** إلى العربية والإنجليزية والإسبانية والصينية.
**صفر** نصوص مُشفّرة ظاهرة للمستخدم بقيت في أي مكوّن.

المكوّنات المُترجمة بالكامل في هذه الجولة:
`AICopilot`, `NotesChat`, `CommandPalette`, `WelcomeSplash`, `ProductivityStats`,
`FocusSprint`, `HabitTracker`, `KnowledgeGraph`, `NotesList`, `BrainDumpMode`,
`FinancialTracker`, `Sidebar`, `SmartClipboard`, `NoteEditor`, `DebtCreditManager`,
`DecisionMatrix`, `PromptModal`.

تحسينات إضافية على ميزات الذكاء الاصطناعي:
- **كشف اللغة الطبيعية متعدد اللغات**: في `FinancialTracker` و`DebtCreditManager`
  و`SmartClipboard` و`BrainDumpMode`، وُسِّعت كلمات الكشف (العملة، الفئة، الدائن/المدين،
  الأولويات) لتشمل الإنجليزية والإسبانية والصينية إلى جانب العربية.
- إصلاح التواريخ المُشفّرة `ar-MA` لتستخدم locale اللغة الحالية.
- حساب «أكثر يوم نشاطاً» ديناميكياً في `ProductivityStats` (كان ثابتاً «الأحد») + إصلاح
  خطأ حرف فيتنامي `để`.

---

## 4) شاشة التطبيق — تناسب جميع قياسات الأجهزة ✅

من أصغر هاتف (≈320px) إلى أكبر لوحة وشاشة خارجية (≥2560px):
- **دعم `safe-area-inset` كامل** على المستوى الجذري (`body`) + أدوات مساعدة
  (`safe-top/bottom/x/all`) للهيدر والفوتر والنوافذ — يحترم النوتش وثقب الكاميرا
  ومؤشّر الـ Home والحواف المستديرة للّوحات.
- **خط متجاوب** عبر `clamp(15px … 18px)` يكبر تدريجياً مع عرض الشاشة، و19px على
  الشاشات الضخمة (≥2000px).
- **منع تكبير iOS التلقائي** عند التركيز على الحقول (الخط ≥ 16px على الأجهزة اللمسية).
- **حدود قصوى للمحتوى** على اللوحات الكبيرة (`content-max`) لتجنّب التمدّد المفرط.
- **الوضع الأفقي منخفض الارتفاع**: تقليل الحشو الرأسي + `max-height` للنوافذ.
- **منع التمرير الأفقي** و **منع النطّ المطّاطي** على iOS داخل WebView.
- النوافذ المنبثقة (`modal-backdrop`) تحترم مناطق الأمان فلا تُقصّ على الأجهزة ذات النوتش.

---

## جولة التدقيق الثانية — تحسينات إضافية

بعد فحص أعمق، عولجت النقاط التالية:

1. **مناطق الأمان للنوافذ ملء الشاشة**: `KnowledgeGraph`، `FocusSprint` (3 شاشات)،
   و`VoiceRecorder` كانت `fixed inset-0` تتجاوز حشو الـ body وقد تُقصّ تحت النوتش.
   أُضيف `safe-all` / `modal-backdrop` لكل منها فلا يختفي أي زر أو رأس خلف النوتش
   أو مؤشّر الـ Home على أي جهاز.
2. **خطأ صوتي مُشفّر**: `speech.ts` كان يعرض «تعذّر بدء الإدخال الصوتي» بالعربية فقط.
   أُضيف مفتاح `start-failed` بأربع لغات ووُجِّه عبر `translateError`.
3. **تحقق توافق البروكسي**: العميل (`geminiService.ts`) يرسل `x-app-token` المطابق
   لحماية `APP_SHARED_SECRET` في الدالة — متوافقان تماماً.
4. **تحقق UMP/ATT/SKAdNetwork**: موافقة GDPR قبل أي إعلان، App Tracking Transparency
   لـ iOS، و SKAdNetwork — كلها سليمة ومتوافقة مع السياسات.
5. **تحقق كشف اللغة الأولية**: يحترم التفضيل المحفوظ ثم لغة الجهاز (عربي/إسباني/صيني)
   ويعود للإنجليزية للمستخدمين الدوليين.

أُضيف أيضاً `دليل_النشر_الكامل.md` يشرح نشر `gemini-proxy` عبر Supabase CLI
خطوة بخطوة، وطريقة تحديث ونشر التطبيق.

## جولة التدقيق الثالثة — أيقونات Apple المعتمة + وضوح الشعار

### إصلاح حرج: أيقونات iOS الشفافة
كانت روابط `apple-touch-icon` و`og:image` و`twitter:image` و`msapplication-TileImage`
في `index.html` تشير إلى أيقونات **شفافة** (`icon-512.png` و`icon-192.png`).
المشكلة: **iOS لا يدعم الشفافية على الشاشة الرئيسية** ويحوّل أي منطقة شفافة إلى
**أسود**، فتظهر الأيقونة بإطار أسود قبيح على أجهزة Apple. وكذلك معاينات المشاركة
الاجتماعية تظهر الشفافية بشكل غير متّسق (أبيض/أسود).

الحل:
- إنشاء أيقونات `apple-touch-icon` **معتمة** مخصّصة (152/167/180px) بخلفية متدرّجة
  بنفسجية داكنة + الشعار، فتظهر احترافية على شاشة iOS الرئيسية.
- إنشاء `social-512.png` معتمة لمعاينات OG/Twitter.
- إنشاء `favicon-32.png` معتمة للمتصفحات.
- تحديث كل الروابط في `index.html` لتستخدم النسخ المعتمة.

> ملاحظة: أيقونات PWA الشفافة (`icon-72…512`) تبقى شفافة لأن purpose=`any`
> على أندرويد يتعامل معها بشكل صحيح، و`maskable-512` معتمة أصلاً.

### وضوح الشعار
أُعيد قص الشعار من المصدر الرمادي (946) بأعلى دقة مع إزالة تلوّث الحواف، وتم
التحقق من الوضوح والنظافة على ثلاث خلفيات (فاتحة، داكنة، رمادية متوسطة) — كل
التفاصيل (الحلزون، الحرف البلوري N، النجمة، الألسنة الملوّنة، النص) واضحة تماماً
بلا أي أثر للنمط الشطرنجي.

### التحقق من احترافية الإعلانات
- البانر يستخدم `ADAPTIVE_BANNER` (يتكيّف مع عرض كل جهاز) في `BOTTOM_CENTER`.
- `AdBanner` يحجز مساحة بارتفاع البانر الحقيقي عبر حدث `SizeChanged` فلا يُغطّى
  أي محتوى — يختفي في وضع المحرّر، ولا يظهر شيء على الويب.
- UMP/GDPR + iOS ATT + SKAdNetwork سليمة.

### التحقق من احترافية الذكاء الاصطناعي
- 16 دالة ذكاء اصطناعي، كلها تحقن `LANG_NAMES[lang]` في المطالبة فترد بلغة
  المستخدم المختارة (عربي/إنجليزي/إسباني/صيني).
- `callGemini` فيه مهلة 30 ثانية مع AbortController، توكن حماية، ومعالجة أخطاء.

## جولة التدقيق الرابعة — توافق API المجاني واحترافية الذكاء الاصطناعي

### تحسين كفاءة الطبقة المجانية (Free Tier)
- **اختيار النموذج الذكي**: 7 دوال خفيفة (الوسوم، العناوين، الأوامر، التدقيق
  الإملائي، استخراج المهام، التلخيص، رسائل الديون) تحوّلت لاستخدام
  `gemini-2.5-flash-lite` الذي له **حدود مجانية أعلى وأسرع** في Google AI Studio،
  بينما المهام الإبداعية الطويلة تبقى على `gemini-2.5-flash`. هذا يضاعف عدد
  الطلبات الممكنة ضمن الحصة المجانية.
- كلا النموذجين متاحان **مجاناً** — متوافق تماماً مع متطلّب "API مجاني".

### معالجة احترافية لنفاد الحصة
- البروكسي يميّز الآن خطأ تجاوز الحصة (429 / RESOURCE_EXHAUSTED) ويرجع رمز
  `QUOTA_EXCEEDED` مع ترويسة `Retry-After: 60`، بدلاً من خطأ عام.
- العميل (`geminiService`) يرمي خطأً مُعرّفاً بهذا الرمز، و`NoteEditor` و`AICopilot`
  يعرضان رسالة "تجاوزت الحد، حاول بعد قليل" بلغة المستخدم.
- **الأهم**: طبقة `aiEngine` فيها fallback محلي مؤكّد لكل دوالها الـ14 — فلو نفدت
  الحصة المجانية، يستمر التطبيق بالعمل بمنطق محلي بدلاً من الفشل الصامت.

### تحديث المراجع القديمة
- أُصلح `SUPABASE_GEMINI_SETUP.md` الذي كان يشير لنموذج `gemini-2.0-flash` المُغلق
  → `gemini-2.5-flash`.

## إصلاح الميكروفون + ميزة التدوين الصوتي الكاملة (صوت + نص)

### السبب الجذري لفشل الميكروفون (مُكتشَف ومُصلَح)
WebView على أندرويد له نظام أذونات بطبقتين منفصلتين تماماً: (1) إذن النظام
`RECORD_AUDIO` في AndroidManifest، و(2) إذن WebView الداخلي عبر
`WebChromeClient.onPermissionRequest()`. منح الطبقة الأولى **لا يكفي أبداً**
— هذا قيد موثَّق في تقارير Capacitor الرسمية منذ 2018 وحتى الآن. الحل:
`android-patches/apply-mic-permission.sh` يُنشئ `MainActivity.java` مخصصة
تُضيف `WebChromeClient` يمنح `RESOURCE_AUDIO_CAPTURE` صراحةً (مع طلب إذن
النظام أولاً إن لم يكن مُمنوحاً). أُدرجت في `.github/workflows/google-play-android.yml`
بعد `apply-admob.sh`.

### ميزة التدوين الصوتي — صوت حقيقي + تحويل لنص معاً
بدل الاعتماد على `webkitSpeechRecognition` غير الموثوقة داخل WebView، يُستخدم
الآن **Gemini نفسه لتحويل الصوت المُسجَّل فعلياً إلى نص**:
1. **التسجيل**: `MediaRecorder` يسجّل صوتاً حقيقياً (`audio/webm;codecs=opus`
   أو احتياطي حسب دعم الجهاز).
2. **الحفظ الدائم**: `@capacitor/filesystem` يكتب الصوت في
   `Directory.Data/voice-notes/` فيبقى صالحاً للاستماع حتى بعد إغلاق التطبيق.
3. **التحويل**: الصوت يُرسل لـ`gemini-proxy` (نموذج `gemini-3.5-flash` مفروض
   إلزامياً للصوت لضمان الدقة، حد 18MB حماية من حد Gemini 20MB)، مع دعم صريح
   للدارجة المغربية بدون ترجمتها للفصحى.
4. **عند فشل التحويل**: الصوت يبقى محفوظاً دائماً مع زر "إعادة محاولة التحويل"
   — لا يُفقَد التسجيل أبداً.
5. **الواجهة**: كل ملاحظة تعرض مُشغِّلات صوت (`<audio controls>`) لكل تسجيل
   مُرفَق، مع زر حذف لكل تسجيل.

## ميزة الحماية برقم سري لملاحظة/فكرة محدَّدة (تشفير حقيقي)

### الحالة عند الفحص
الميزة كانت مُطبَّقة فعلياً بالكامل (`noteLock.ts` + `NoteLockModal.tsx` +
تكامل في `NoteEditor.tsx`) — تشفير **AES-GCM-256 حقيقي** مع `PBKDF2`
(100,000 تكرار) عبر Web Crypto API المدمجة، لا قفل واجهة شكلي قابل للتجاوز
بفحص `localStorage`. الرقم السري نفسه **لا يُخزَّن أبداً** — فك التشفير
يفشل تلقائياً برقم خاطئ بفضل خاصية tag في GCM.

### أخطاء حرجة وُجدت وأُصلحت
1. **استيراد مكرر بالكامل** في `NoteEditor.tsx` (`NoteLockModal` و`noteLock`
   مُستورَدان مرتين) — كان سيُسقط البناء بخطأ "Duplicate identifier" فوراً.
2. **حقول ميتة مكرَّرة** في `Note` interface (`locked`/`lockSalt`/`lockIv`
   القديمة لم تُستخدَم في أي مكان، بجانب `isLocked`/`lock.salt`/`lock.iv`
   الفعلية المُستخدَمة) — أُزيلت الحقول الميتة.
3. **تسريب محتوى مُشفَّر لـ Gemini**: ميزات الذكاء الاصطناعي (`AICopilot`
   محادثة + تنظيم الملاحظات، `NotesChat`) كانت تُرسل المحتوى المُشفَّر
   (نص عشوائي base64) للملاحظات المقفلة كسياق للـ API — يُهدر الحصة
   المجانية بلا فائدة ويُخالف منطق الحماية. أُضيف `!n.isLocked` لكل نقاط
   بناء السياق.
4. **بحث لوحة الأوامر** (`CommandPalette.tsx`) كان يبحث في المحتوى المُشفَّر
   — أُصلح للبحث في العنوان/الوسوم فقط للملاحظات المقفلة.
5. **معاينة محتوى مُشفَّر في `NotesChat`**: الردود المحلية (البحث عن
   "أفكار"/"لعبة") كانت تعرض المحتوى المُشفَّر كمعاينة نصية — استُبدلت
   برمز 🔒 للملاحظات المقفلة.

### غير مُتأثِّر (كان محصَّناً مسبقاً)
- `NotesList.tsx` (البحث) و`backlinks.ts` (الروابط المرتدّة) كانا يستثنيان
  المحتوى المُشفَّر بشكل صحيح من قبل.

## ميزة الفقعة العائمة (تطفو فوق كل التطبيقات) — Android فقط

### القرار التصميمي الأهم: أمان البيانات أولاً
لا توجد أي كتابة مباشرة من Kotlin/Java لقاعدة بيانات WebView — خطر حقيقي
على سلامة الملاحظات. عند اختيار أي بند من قائمة الفقعة، يُسجَّل الإجراء
ويُفتح التطبيق نفسه فوراً على الشاشة المطلوبة، فتتم الكتابة عبر نظام
الحفظ الموجود والمُختبَر (مع AI، التنبيهات، القفل).

### المكوّنات
- `BubbleOverlayPlugin.java`: جسر Capacitor (check/request permission،
  start/stop، isActive، getPendingAction).
- `BubbleService.java`: خدمة Foreground (إلزامية لأي عرض عائم على
  أندرويد) — تبني أيقونة دائرية + قائمة منبثقة (📝 ملاحظة سريعة /
  💬 محادثة AI / ⏰ تذكير / ✕ إيقاف) برمجياً بالكامل، بدون أي ملفات
  XML/drawable إضافية (تفادياً لمخاطر دمج الموارد التي واجهناها سابقاً).
  متوافقة مع Android 14+ عبر `FOREGROUND_SERVICE_TYPE_SPECIAL_USE`.
- `MainActivity.java`: نسخة شاملة تضم إصلاح الميكروفون السابق + تسجيل
  الإضافة + حقل `pendingBubbleAction` (يُستهلَك مرة واحدة من JS).
- `apply-bubble-overlay.sh`: يُولِّد كل الملفات أعلاه + يُحدِّث
  `AndroidManifest.xml` (أذونات + تعريف `<service>`).
- `bubbleOverlay.ts` (JS): جسر TypeScript يتحقق من المنصة (Android فقط)
  ويُرجع قيماً آمنة على غير أندرويد بدل رمي أخطاء.
- زر تفعيل في `Sidebar.tsx` يطلب الإذن (تحويل لإعدادات النظام عند
  الحاجة، لا يمكن طلبه تلقائياً) ويُشغِّل/يوقف الخدمة.
- `App.tsx` يستهلك `pendingBubbleAction` عند الإقلاع وعند كل عودة من
  الخلفية، ويوجِّه لشاشة الملاحظة الجديدة أو محادثة AI تلقائياً.

### تحذيرات صريحة (نفس ما أُوضِّح للمستخدم قبل البناء)
- iOS لا يدعم هذا إطلاقاً (قيود Apple).
- إذن `SYSTEM_ALERT_WINDOW` يحتاج تفعيلاً يدوياً من إعدادات النظام.
- إشعار دائم إلزامي طالما الفقعة نشطة (شرط نظام أندرويد).
- مخاطرة محتملة في مراجعة Google Play (إذن حسّاس تاريخياً).
- لم تُختبَر بصرياً على جهاز حقيقي (لا تتوفر بيئة اختبار هنا) — تحقّق
  التوازن النحوي للكود فقط (أقواس/أقواس معقوفة)، وقد تحتاج جولة تصحيح
  بعد أول بناء فعلي على جهازك.

## التدوين الصوتي — يعمل بالإنترنت وبدونه

### الواقع التقني (بصراحة)
لا توجد طريقة موثوقة ومجانية لتحويل الصوت إلى نص **بدقة جيدة بدون إنترنت**
على الأجهزة الهجينة (WebView) — `webkitSpeechRecognition` نفسها تعتمد على
خوادم Google السحابية في أغلب التطبيقات، فلا تعمل أصلاً بدون إنترنت. الحل
الواقعي والمحترف المُطبَّق:

| الخطوة | يعمل بدون إنترنت؟ |
|---|---|
| 🎙️ التسجيل (`MediaRecorder`) | ✅ نعم دائماً |
| 💾 الحفظ الدائم (`@capacitor/filesystem`) | ✅ نعم دائماً |
| 📝 التحويل لنص (Gemini) | ❌ يحتاج إنترنت — **لكنه يُؤجَّل تلقائياً** |

### آلية التأجيل والإعادة التلقائية
- عند بدء التسجيل بدون إنترنت: لا محاولة اتصال أصلاً (توفير بطارية/وقت)،
  رسالة واضحة "📡 لا إنترنت — تم حفظ الصوت، سيُحوَّل تلقائياً عند الاتصال".
- التسجيل يُحفَظ بعلامة `pendingTranscription: true` + `path` (المسار الخام
  لإعادة قراءته من القرص لاحقاً، حتى بعد إغلاق التطبيق وإعادة فتحه).
- `voiceRetryQueue.ts` (جديد) يعمل في الخلفية عبر `AppContext`: محاولة عند
  تشغيل التطبيق + عند كل حدث `online` من المتصفح — يقرأ الصوت من القرص،
  يحوّله، يُلحِق النص بالملاحظة، ويُزيل علامة الانتظار.
- زر "إعادة المحاولة الآن" متاح أيضاً يدوياً على كل شريحة صوت معلَّقة داخل
  المحرّر (دون انتظار حدث `online`).
- الملاحظات **المقفلة** بالرقم السري تُستثنى من إعادة المحاولة التلقائية
  (لا يمكن الوصول لمحتواها الصريح بدون الرقم السري في الخلفية).

## الفقعة العائمة (Android فقط) — تذكير سريع + تدوين سريع + محادثة AI

### الحالة عند الفحص
الميزة كانت مُطبَّقة بالفعل بالكامل ومتصلة بكل طبقاتها: `bubbleOverlay.ts`
(جسر JS↔Native)، `android-patches/apply-bubble-overlay.sh` (يُولِّد
`BubbleOverlayPlugin.java` + `BubbleService.java` + نسخة شاملة من
`MainActivity.java` تضم إصلاح الميكروفون + تسجيل البلوغن معاً)، متصلة
بالكامل في `App.tsx` (استهلاك الإجراء المُؤجَّل عند الإقلاع والاستئناف)
و`Sidebar.tsx` (مفتاح تبديل كامل مع معالجة كل الحالات).

### القرار التصميمي الأهم (أمان البيانات)
لا تتم أي كتابة بيانات من Kotlin/Java مباشرة إلى قاعدة بيانات WebView
(`localStorage`) — خطر حقيقي على سلامة الملاحظات. الضغط على أي خيار في
قائمة الفقعة (📝 ملاحظة سريعة / 💬 محادثة AI / ⏰ تذكير) **يفتح التطبيق
نفسه** فوراً على الشاشة المطلوبة، فتتم الكتابة عبر نظام الحفظ الموجود
والمُختبَر بالفعل (بكل ميزاته: AI، تنبيهات، قفل).

### تحقق فعلي (محاكاة بناء حقيقية)
شُغِّل تسلسل السكريبتات الفعلي (`apply-mic-permission.sh` ثم
`apply-bubble-overlay.sh`) في بيئة محاكاة بمشروع أندرويد وهمي، وتحقَّقنا من:
توازن الأقواس/الأقواس الهلالية في الملفات الثلاثة الناتجة (سليمة 100%)،
عدم وجود متغيرات bash متبقية بالخطأ، صحة XML النهائي لـ`AndroidManifest.xml`
(الأذونات الثلاثة + تعريف `<service>` بـ`foregroundServiceType="specialUse"`
المطلوب على أندرويد 14+). تأكَّدنا أيضاً أن ترتيب السكريبتين في
`google-play-android.yml` صحيح (الفقعة تُشغَّل **بعد** الميكروفون) لأن كلا
السكريبتين يكتب `MainActivity.java` — الفقعة تكتب النسخة الشاملة الأخيرة
فلا يضيع إصلاح الميكروفون.

### حدود الميزة (Android فقط)
- iOS لا يدعم النوافذ العائمة فوق التطبيقات الأخرى إطلاقاً (قيود Apple)
- إذن "العرض فوق التطبيقات الأخرى" يدوي حصرياً من إعدادات النظام
- إشعار دائم إلزامي طالما الفقعة نشطة (شرط نظام أندرويد، غير اختياري)
- لا يمكن اختبارها بصرياً من بيئة العمل الحالية — أول تجربة فعلية من جهاز
  المستخدم، قد تحتاج جولة تصحيح بعد أول بناء حقيقي

### [FIX] متانة تمرير الإجراء من الخدمة للنشاط
كان `BubbleService.handleAction()` يضبط `MainActivity.pendingBubbleAction`
كحقل ثابت مباشرة (يعمل فعلاً بفضل بقاء العملية حيّة كـForeground Service)،
لكن `MainActivity.captureBubbleAction()` كان يقرأ من `Intent extra` غير
مُعرَّف أصلاً — كود ميت لا يُسبب عطلاً حالياً لكنه يعتمد على افتراض ضمني
غير موثَّق. أُضيف `launch.putExtra("bubble_action", action)` صريحاً لجعل
الآلية متينة وواضحة المصدر بدل الاعتماد على ترتيب تنفيذ ضمني فقط.

## التحقق النهائي

- ✅ فحص نحوي (`tsc`) ناجح على **كل** ملفات `src` بلا أخطاء.
- ✅ مفاتيح `i18n.ts` متطابقة عبر اللغات الأربع (108 لكل لغة).
- ✅ كل الأيقونات بالأبعاد والشفافية الصحيحة.
- ✅ صفر نصوص عربية مُشفّرة ظاهرة للمستخدم في كامل المشروع.
- ✅ هيكل المشروع محفوظ، الاسم: **Notic Tahiro**.

---

## تحديث تكرار الإعلانات (بطلب صريح من المستخدم)

⚠️ **تنبيه مُسجَّل:** التغييرات التالية تخالف توصية AdMob الرسمية الموثَّقة
("Apps should not trigger an interstitial ad every time a user clicks" —
support.google.com/admob/answer/6201362). نُفِّذت بطلب المستخدم الصريح
لتطبيقه الخاص بعد التنبيه.

1. **عند الإغلاق:** الإعلان البيني يظهر الآن عند **كل** خروج من ملاحظة/فكرة
   (كان كل 3 خروجات).
2. **عند الفتح:** إعلان بيني جديد عند **فتح** ملاحظة/فكرة (جديدة أو موجودة)
   كل 3 فتحات — `useEffect` جديد في `NoteEditor.tsx` بعداد `sessionStorage`
   باسم `_nt_open`، ينفّذ مرة واحدة فقط عند mount.
3. **App Open:** الفاصل قُلِّص من 4 ساعات إلى **30 دقيقة**
   (`APP_OPEN_INTERVAL` في `admob.ts`).

**الحماية التقنية المتبقية:** `INTERSTITIAL_MIN_GAP` (45 ثانية) في `admob.ts`
يبقى نشطاً كحاجز أخير يمنع عرض إعلانين متتاليين في حال إغلاق وفتح سريعين جداً.

**يُنصح:** بمراقبة لوحة AdMob (Policy Center) عن قرب أول أسبوعين بعد النشر
للتأكد من عدم تسجيل أي تحذير سياسة.
